# Copy this file to .env and fill in your key (for local dev with `npm run dev`).
# On Netlify, set GEMINI_API_KEY under Site settings → Environment variables instead.

GEMINI_API_KEY=your-gemini-api-key-here

# Optional — defaults to gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash
